## 产物不满意

1. **二次验证码输入框使用密码圆点模式，输入时无法看到数字**
复现步骤：打开登录页，用异常账号触发风控拦截，弹出"登录异常预警"弹窗后，在"安全验证"区域的输入框中输入6位验证码。
判定原因：输入框加了 show-password 属性，输入内容显示为圆点。验证码是纯数字短串，用户需要看到已输入的内容来确认是否输错，密码遮盖在这里没有安全意义，反而让用户不知道自己输到哪一位了。

2. **批量清除风险操作不弹出备注输入框，直接提交固定字符串**
复现步骤：在登录日志页勾选多条记录，点击"批量清除风险"，确认弹窗点确定后直接完成，没有让用户填写清除原因的环节。
判定原因：单条清除风险时弹窗让用户填写备注说明，但批量清除风险时确认后直接调用接口传了写死的"批量清除风险标记"。同一个功能、同一个页面上，单条和批量两条操作路径的行为不一致。

3. **生成风控报告时设备、IP、状态等筛选条件未传入接口**
复现步骤：在登录日志页筛选条件中填入设备名称、IP地址、选择登录状态和风险等级，点击"生成风控报告"，查看生成的报告内容。
判定原因：生成报告时只提取了 timeRange 时间范围参数，filterForm 中的 loginDevice、loginIp、status、riskLevel 四个字段都没有拼到请求参数里。用户设置了筛选条件但报告拿到的却是全量数据，筛选条件白设了。

---

## 过程不满意

1. 登录页二次验证输入框的 show-password 属性在实现时直接写进去了，整个过程中没有人对这个输入框的交互方式做过检查，验证码用密码模式这个明显不合理的地方没有被发现。对应产物不满意第1条。

2. 批量清除风险操作的前端逻辑和单条清除在同一个文件里，单条清除写了备注弹窗，批量清除直接硬编码备注字符串，两段代码之间没有做行为一致性比对。对应产物不满意第2条。

3. 生成风控报告时只传了时间范围，filterForm 里其他四个筛选字段在拼参步骤被遗漏了，这部分参数传递逻辑在实现时没有完整覆盖到所有筛选条件。对应产物不满意第3条。

---

## 任务完成情况评价

整体完成度约 80%。四个新增功能点的主流程都跑通了，页面能展示、接口能调通，但在交互细节一致性上掉了链子——单条和批量操作路径行为不统一、筛选条件没有完整透传到关联功能，这类问题在实现后的自验环节没有被兜住。
